# Minimal dependencies for PRISM inference when run from the main repo (versatilemotion).
# Install from versatilemotion root: pip install -r opensource/prism/requirements.txt
torch>=1.13
transformers>=4.30
diffusers>=0.25
einops
safetensors
# Required by mmotion (main repo)
mmengine
